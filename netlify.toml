[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so React Router paths like /reset-password/:token work on refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
